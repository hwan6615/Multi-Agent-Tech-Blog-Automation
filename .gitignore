# --- Byte-compiled / optimized / DLL files ---
__pycache__/
*.py[cod]
*$py.class

# --- Virtual Environments (uv & general) ---
.venv/
venv/
ENV/
env/

# --- Environment Variables (Security) ---
# API 키가 포함된 이 파일은 절대로 GitHub에 올라가면 안 됩니다.
.env
.env.local
.env.shared

# --- Streamlit ---
# secrets.toml 같은 보안 파일이 올라가는 것을 막기 위해 폴더 전체를 무시하고
# 디자인 설정인 config.toml만 예외적으로 허용하는 좋은 설정입니다.
.streamlit/
! .streamlit/config.toml

# --- Python build / distribution ---
dist/
build/
*.egg-info/

# --- Data & Logs ---
*.log
data/
outputs/

# --- OS specific files ---
.DS_Store
Thumbs.db
.idea/
.vscode/

# --- Milvus & Docker Volumes (V2 필수 추가!) ---
# Docker가 생성하는 DB 데이터 파일들입니다. 용량이 크니 꼭 제외하세요.
volumes/
milvus_memory.db